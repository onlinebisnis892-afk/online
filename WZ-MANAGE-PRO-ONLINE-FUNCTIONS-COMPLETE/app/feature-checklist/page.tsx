'use client';import {useState} from 'react';import {allFeatures} from '@/lib/feature-registry';
export default function Checklist(){const [q,setQ]=useState('');const [done,setDone]=useState<string[]>([]);const filtered=allFeatures.filter(x=>x.feature.toLowerCase().includes(q.toLowerCase())||x.moduleName.toLowerCase().includes(q.toLowerCase()));return <><h1>Master Feature Checklist</h1><div className="toolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Cari fitur..."/><span className="pill">✅ {done.length} / {allFeatures.length}</span></div><div className="card">{filtered.map(x=><label className="feature" key={x.module+'-'+x.feature}><input type="checkbox" checked={done.includes(x.feature)} onChange={()=>setDone(d=>d.includes(x.feature)?d.filter(v=>v!==x.feature):[...d,x.feature])}/> {x.moduleName} — {x.feature}</label>)}</div></>}

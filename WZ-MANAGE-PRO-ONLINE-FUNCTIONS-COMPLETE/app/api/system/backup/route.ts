import {NextResponse} from 'next/server';import {getSessionUser} from '@/lib/auth';import {createBackup} from '@/lib/backup';import {db} from '@/lib/db';export async function GET(){const u=await getSessionUser();if(!u||u.role!=='OWNER')return NextResponse.json({message:'Owner only'},{status:403});const backup=await createBackup();await db.backupLog.create({data:{type:'MANUAL',location:'generated-json'}});return NextResponse.json(backup)}

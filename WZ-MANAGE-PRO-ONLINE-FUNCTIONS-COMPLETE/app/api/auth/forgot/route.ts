import {NextRequest,NextResponse} from 'next/server';import {db} from '@/lib/db';import {createToken,hashToken} from '@/lib/security';
export async function POST(req:NextRequest){const {username}=await req.json();const u=await db.user.findUnique({where:{username}});if(!u)return NextResponse.json({success:true});const raw=createToken();await db.auditLog.create({data:{userId:u.id,action:'PASSWORD_RESET_REQUEST',details:{tokenHint:raw.slice(0,6)}}});return NextResponse.json({success:true,message:'Token reset dibuat untuk alur server. Untuk keamanan, token harus dikirim melalui kanal terverifikasi.'})}
